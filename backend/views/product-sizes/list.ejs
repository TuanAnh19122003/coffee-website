<!DOCTYPE html>
<html lang="en">
    <%- include('../layout/head.ejs', {pageTitle: 'Product Size Management'}) %>
<body>
    <div id="wrapper">
        <%- include('../layout/sidebar.ejs') %>
        <div id="content-wrapper" class="d-flex flex-column">
            <!-- Main Content -->
            <div id="content">
                <!-- navbar.ejs -->
                <%- include('../layout/navbar.ejs') %>
                <div class="card mt-2" style="margin: 10px;">
                    <div class="card-header d-flex align-items-center justify-content-between mb-4">
                        <h1 class="h3 mb-0 text-gray-800">Product Size List</h1>
                        <a href="/product-sizes/create" class="btn btn-primary shadow-sm">
                            <i class="fa-solid fa-circle-plus"></i> New
                        </a>
                    </div>
                    <div class="card-body">
                        <div class="table-responsive">
                            <table class="table table-striped table-hover">
                                <thead>
                                    <tr>
                                        <th>STT</th>
                                        <th>Size</th>
                                        <th>Price</th>
                                        <th>Product</th>
                                        <th class="text-center">Action</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <% product_sizes.forEach((product_size,index )=> { %>
                                        <tr>
                                            <!-- <td><%= product_size.id %></td> -->
                                            <td><%= index + 1 %></td>
                                            <td><%= product_size.size %></td>
                                            <td><%= product_size.price %></td>
                                            <td><%= product_size.product ? product_size.product.name : 'No product' %></td>
                                            <td class="text-center">
                                                <a href="/product-sizes/<%= product_size.id %>/edit" class="btn btn-outline-warning btn-sm me-1">
                                                    <i class="fa-solid fa-pen-to-square"></i> Edit
                                                </a>
                                                <a href="/product-sizes/<%= product_size.id %>/delete" class="btn btn-outline-danger btn-sm me-1" onclick="return confirm('Bạn có chắc muốn xóa kích thước này?');">
                                                    <i class="fa-solid fa-trash"></i> Delete
                                                </a>
                                                <a href="/product-sizes/<%= product_size.id %>/detail" class="btn btn-outline-info btn-sm">
                                                    <i class="fa-solid fa-circle-info"></i> Details
                                                </a>
                                            </td>
                                        </tr>
                                    <% }) %>
                                </tbody>
                            </table>
                        </div>
                        
                    </div>
                    <!-- pagination -->
                    <%- include('../layout/pagination.ejs') %>
                </div>
            </div>
            <!-- Footer -->
            <%- include('../layout/footer.ejs') %>
        </div>
    </div>
    
    <!-- tail -->
    <%- include('../layout/tail.ejs') %>
</body>
</html>
